# V2.0.0-dev 生产部署验收（不可变快照，2026-09-11 11:57）

> 部署对象：Task 1–8 全部完成后的 V2.0.0-dev（commit `7899559`）到生产 `https://taoxie.vip/digital-avatar/`（Ubuntu `43.143.231.106`，node v22.19.0 / npm 10.9.3，无 pnpm）。
> 本文为带时间戳的不可变验收副本；CHANGELOG 同步条目见同日 `20260911_1157_CHANGELOG_V2.0.0-dev-生产部署.md`。

## 1. 部署方式（生产机非 git 仓库，沿用 scp+tar 流程）

1. 本地构建三产物：`shared` tsc 后 `npm pack`（`digital-avatar-shared-1.0.0.tgz`，20KB）、`client` `pnpm build` 后 tar dist（30MB）、`server/src` + package.json tar（15KB）
2. scp 到 `/tmp/`；服务器停 tmux `davatar`，备份 `src.bak.20260911-114749`、`package.json(.lock).bak`、DB 三件套 `db.bak.20260911-114749/`
3. shared workspace 依赖无 pnpm 解法：tgz 落 `server/vendor/shared.tgz`，`npm install ./vendor/shared.tgz --omit=dev`（部署机 package.json 依赖被改写为 file: 路径，21 个导出导入成功）
4. 新 tmux `davatar` 执行 `/home/ubuntu/digital-avatar/run-davatar.sh`（本次新建，纯 LF，append server.log），进程 pid 408752，PPID=tmux pane
5. dist 原子替换，旧版备份 `client/dist.bak.20260911-115312`（保留最近 3 份）

## 2. 验收记录

| #   | 验收项                    | 结果 | 证据（2026-09-11）                                                                                              |
| --- | ------------------------- | ---- | --------------------------------------------------------------------------------------------------------------- |
| P1  | HTTPS 入口与启动分包      | ✅   | `GET /digital-avatar/` 200 1241B；`index-B5Z_ig7S.js` 200 3338B；react/live2d/AppStage/net/Admin chunk 全 200    |
| P2  | 资源头                    | ✅   | JS `Content-Type: application/javascript`；`cache-control: public,max-age=2592000,immutable`（带 hash 长缓存） |
| P3  | 模型/Runtime              | ✅   | `models/hiyori/Hiyori.model3.json` 200；`live2d/live2dcubismcore.min.js` 200                                    |
| P4  | Service Worker            | ✅   | `sw.js` 200，内容为 `da-cache-v2.0.0`                                                                           |
| P5  | API 经 nginx              | ✅   | `GET /api/health` → `{"ok":true}` 200                                                                           |
| P6  | WebSocket 链路            | ✅   | `/socket.io/?EIO=4&transport=polling` 200；浏览器实测升级链路无报错                                              |
| P7  | V2 路由面                 | ✅   | `GET /api/quests` 404（Task7 已删）；`POST /api/unbind` 非 404（新路由已挂载）                                   |
| P8  | DB 迁移                   | ✅   | 表：bonds/events/memories/shared_moments/states/users/schema_migrations + 旧 growth_events 原地保留只读         |
| P9  | 互动闭环（真机浏览器）    | ✅   | 点击「摸摸头」→ `POST /api/interact` 200；events 新行 `pat` 11:55:57；日志 `milestone unlocked: first_interaction`；旧数据（09-07 行）完好 |
| P10 | 前端控制台                | ✅   | 无 error/warn，无非 2xx 请求；无白屏、无启动失败遮罩；舞台/气泡/Dock 正常                                       |
| P11 | 懒加载后台                | ✅   | `#/admin` Admin chunk 打开正常，返回首页正常                                                                    |
| P12 | 服务进程                  | ✅   | tmux `davatar` 托管 pid 408752，与 ssh 通道无关；server.log error/exception 计数 0                               |

## 3. 回滚预案（如发现问题）

```bash
# 前端（秒级）
cd /home/ubuntu/digital-avatar/client
rm -rf dist && mv dist.bak.20260911-115312 dist
# 后端
cd /home/ubuntu/digital-avatar/server
tmux kill-session -t davatar
rm -rf src && mv src.bak.20260911-114749 src
cp package.json.bak.20260911-114749 package.json
cp package-lock.json.bak.20260911-114749 package-lock.json
npm install --omit=dev --loglevel=error   # 恢复旧依赖树
tmux new-session -d -s davatar /home/ubuntu/digital-avatar/run-davatar.sh
# 数据库：V2 未做破坏性表变更（仅新增表+幂等 ALTER），一般无需回滚；确需时停服后用 db.bak.20260911-114749/ 三件套覆盖
```

## 4. 注意事项

- 生产机无 git、无 pnpm：后续服务端发布继续走"scp tar + npm install vendor/shared.tgz"流程；`server/vendor/shared.tgz` 不可删（file: 依赖指向它）
- Jing/Tao 仍是占位包，发布门禁 `verify-avatar-contract.mjs --release` 继续阻断；线上使用旧四模型，不受影响
- SW 缓存版本已升 v2.0.0：老用户下次访问自动接管，离线壳为本次新版
